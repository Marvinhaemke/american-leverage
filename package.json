{
  "name": "american-leverage",
  "version": "1.0.0",
  "private": true,
  "description": "American Leverage landing page (static site + Meta CAPI serverless function), hosted on Netlify.",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  }
}
